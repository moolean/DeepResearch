# Example configuration for using remote API (e.g., OpenRouter)
# Copy this to .env and modify with your actual values

# =============================================================================
# Remote API Configuration
# =============================================================================
USE_REMOTE_API=true
INFERENCE_API_BASE=https://openrouter.ai/api/v1
INFERENCE_API_KEY=your_openrouter_api_key_here
MODEL_PATH=alibaba/tongyi-deepresearch-30b-a3b

# =============================================================================
# Dataset and Output Configuration
# =============================================================================
DATASET=eval_data/your_questions.jsonl
OUTPUT_PATH=./outputs

# =============================================================================
# Inference Configuration
# =============================================================================
ROLLOUT_COUNT=3
TEMPERATURE=0.85
PRESENCE_PENALTY=1.1
MAX_WORKERS=30
MAX_LLM_CALL_PER_RUN=100

# =============================================================================
# Tool Selection - Choose which tools to enable
# =============================================================================
# Available tools: search, visit, google_scholar, PythonInterpreter, parse_file
# Example: only enable search and visit for faster, focused inference
ENABLED_TOOLS=search,visit,google_scholar,PythonInterpreter

# =============================================================================
# API Keys for Tools
# =============================================================================
# Serper API for web search and Google Scholar
SERPER_KEY_ID=your_serper_key

# Jina API for web page reading
JINA_API_KEYS=your_jina_key

# Summary model API (OpenAI-compatible) for page summarization
API_KEY=your_summary_api_key
API_BASE=your_summary_api_base
SUMMARY_MODEL_NAME=your_summary_model_name

# =============================================================================
# Optional: Custom Prompts (uncomment to override defaults)
# =============================================================================
# SYSTEM_PROMPT="Your custom system prompt here..."
# EXTRACTOR_PROMPT="Your custom extractor prompt here..."

# =============================================================================
# Feature Flags
# =============================================================================
QWEN_DOC_PARSER_USE_IDP=false
QWEN_IDP_ENABLE_CSI=false
NLP_WEB_SEARCH_ONLY_CACHE=false
NLP_WEB_SEARCH_ENABLE_READPAGE=false
NLP_WEB_SEARCH_ENABLE_SFILTER=false
QWEN_SEARCH_ENABLE_CSI=false
SPECIAL_CODE_MODE=false
PYTHONDONTWRITEBYTECODE=1
